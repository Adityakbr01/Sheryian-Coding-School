{
  "name": "sherymemory-extension",
  "version": "1.0.0",
  "scripts": {
    "build": "npx -y esbuild src/popup/popup.ts --bundle --outfile=dist/popup.js --format=iife --target=chrome100 && npx -y esbuild src/background/service-worker.ts --bundle --outfile=dist/service-worker.js --format=iife --target=chrome100 && npx -y esbuild src/content/content.ts --bundle --outfile=dist/content.js --format=iife --target=chrome100",
    "dev": "npm run build"
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.268",
    "bun-types": "latest",
    "esbuild": "^0.27.4",
    "typescript": "^5"
  }
}
